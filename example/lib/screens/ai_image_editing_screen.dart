import 'package:flutter/material.dart';

import 'common/ai_feature_scaffold.dart';

class AiImageEditingScreen extends StatelessWidget {
  const AiImageEditingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AiFeatureScaffold(
      title: 'Image Editing (Coming Soon)',
      result: 'Inpainting / editing logic will go here!',
    );
  }
}
